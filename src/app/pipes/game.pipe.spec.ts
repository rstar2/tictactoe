/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { GamePipe } from './game.pipe';

describe('GamePipe', () => {
  it('create an instance', () => {
    let pipe = new GamePipe();
    expect(pipe).toBeTruthy();
  });
});
